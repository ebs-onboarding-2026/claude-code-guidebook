# 클로드 코드 입문자 가이드북

> 설치부터 첫 번째 결과물까지, 직접 따라 하며 배우는 Claude Code
>
> 대상: AI 코딩 도구를 처음 사용하는 사람 · 기준일: 2026-09-14
> Windows PowerShell 중심이며 macOS·Linux 설치 명령도 함께 제공합니다.

## 목차

1. 클로드 코드 이해하기
2. 설치하고 로그인하기
3. 20분 실습: 나만의 소개 페이지
4. 원하는 결과를 얻는 요청법
5. 자주 쓰는 명령어
6. CLAUDE.md로 작업 규칙 정하기
7. 기존 프로젝트에서 작업하기
8. 문제 해결과 사용량 관리
9. 일주일 학습 계획
10. 한 장 체크리스트

## 1. 클로드 코드 이해하기

Claude Code는 Anthropic의 AI 코딩 도구입니다. 프로젝트 파일을 읽고, 코드를 수정하고, 필요한 명령을 실행하는 작업을 자연어로 요청할 수 있습니다. 이 가이드는 터미널에서 사용하는 방식을 다룹니다. [공식 시작 가이드](https://code.claude.com/docs/en/quickstart)

예를 들어 다음처럼 요청합니다.

```text
이 프로젝트의 목적과 폴더 구조를 초보자에게 설명해줘.
수정은 하지 말고, 실행 방법부터 알려줘.
```

처음에는 아래 용어만 알아도 충분합니다.

| 용어 | 쉬운 설명 |
| --- | --- |
| 터미널 | 글자로 명령을 입력하는 프로그램 |
| 프로젝트 폴더 | 작업할 코드와 자료가 모인 폴더 |
| 프롬프트 | AI에게 전달하는 요청문 |
| CLI | 터미널에서 명령으로 사용하는 방식 |
| diff | 수정 전후의 차이 |
| Git | 파일의 변경 이력을 관리하는 도구 |
| 테스트 | 프로그램이 의도대로 동작하는지 확인하는 과정 |

중요한 습관은 **작게 요청하고, 결과를 실행해서 확인하는 것**입니다. “완료했다”는 답변과 실제로 원하는 기능이 동작하는지는 따로 확인하세요.

## 2. 설치하고 로그인하기

### 준비물

인터넷 연결과 Claude Code 사용 권한이 있는 계정이 필요합니다. 공식 문서에는 Pro·Max·Team·Enterprise 또는 Console 계정 등이 안내되어 있습니다. 무료 Claude 계정만으로 사용할 수 있다고 가정하지 마세요. 구독 이용과 Console API 과금은 구분해서 확인하세요. [설치 및 계정 안내](https://code.claude.com/docs/en/setup)

### Windows PowerShell

시작 메뉴에서 **PowerShell**을 실행하고 아래 공식 설치 명령을 입력합니다. 공식 서버에서 설치 스크립트를 내려받아 실행하는 명령입니다.

```powershell
irm https://claude.ai/install.ps1 | iex
```

설치가 끝나면 새 PowerShell 창을 열고 확인합니다.

```powershell
claude --version
```

버전이 표시되면 설치가 된 것입니다. 현재 공식 문서상 Windows에서는 Git for Windows가 선택 사항이며, 설치하면 Git Bash를 사용할 수 있습니다. [Windows 설치 안내](https://code.claude.com/docs/en/setup)

### macOS·Linux·WSL

해당 환경의 터미널에서 실행합니다.

```bash
curl -fsSL https://claude.ai/install.sh | bash
claude --version
```

PowerShell용 명령과 Bash용 명령을 섞어 사용하지 마세요. [공식 설치 명령](https://code.claude.com/docs/en/quickstart)

### 첫 실행과 로그인

실습용으로 사용할 상위 폴더에서 아래 명령을 한 줄씩 실행합니다. 같은 이름의 폴더가 이미 있다면 다른 이름을 사용하세요.

```powershell
mkdir claude-first-project
cd claude-first-project
claude
```

처음 표시되는 안내에 따라 계정에 로그인합니다. 작업 폴더를 신뢰할지 묻는 화면이 나오면 경로를 확인하세요. [첫 실행 안내](https://code.claude.com/docs/en/quickstart)

**입력 위치를 구분하세요.** `claude`는 PowerShell 같은 터미널에 입력합니다. 실행 후에는 Claude Code 입력창에 한국어 요청을 입력합니다. 터미널로 돌아가려면 `/exit`를 입력합니다.

## 3. 20분 실습: 나만의 소개 페이지

별도의 웹 프레임워크 없이 HTML 파일 하나로 시작합니다. 아래 시간은 설치를 마친 뒤의 대략적인 학습 시간입니다.

### 1단계: 만들 결과 정하기 — 3분

Claude Code 입력창에 붙여 넣으세요.

```text
나는 코딩 입문자야. 이 폴더에서 자기소개 웹페이지를 만들려고 해.
아직 파일을 만들지 말고, 아래 요구사항을 구현할 계획을 짧게 설명해줘.

- index.html 파일 하나로 구성
- HTML, CSS, 기본 JavaScript만 사용
- 이름, 소개, 관심사 3개를 표시
- 밝은 테마와 어두운 테마를 바꾸는 버튼
- 휴대폰 화면에서도 읽기 편한 구성
- 외부 서비스, 로그인, 유료 API는 사용하지 않음
```

계획에 모르는 용어가 나오면 “그 용어를 일상적인 예시로 설명해줘”라고 요청하세요.

### 2단계: 파일 만들기 — 5분

```text
위 계획대로 index.html을 만들어줘.
이름은 '홍길동', 소개는 '새로운 것을 배우는 예비 개발자'로 넣어줘.
관심사는 독서, 여행, 코딩이야.
완료하면 파일을 여는 방법과 확인할 항목을 알려줘.
```

작업 중 승인 화면이 나타나면 실행할 명령과 수정할 파일을 읽고 결정합니다. 승인 요청 여부는 권한 모드와 설정에 따라 달라집니다. 모든 수정 전에 반드시 질문한다고 가정하지 마세요. `Shift+Tab`으로 권한 모드를 전환할 수 있습니다. [권한 모드 안내](https://code.claude.com/docs/en/quickstart)

### 3단계: 브라우저에서 확인하기 — 5분

파일 탐색기에서 실습 폴더의 `index.html`을 더블클릭합니다. 페이지가 열리면 다음을 확인하세요.

- 이름과 소개가 정확한가?
- 테마 버튼을 누르면 배경과 글자색이 바뀌는가?
- 어두운 테마에서도 글자를 읽기 쉬운가?
- 창 너비를 줄여도 내용이 화면 밖으로 넘어가지 않는가?

문제가 있으면 관찰한 현상을 그대로 알려주세요.

```text
브라우저에서 확인했는데 어두운 테마의 소개 문장이 잘 보이지 않아.
어두운 테마에서 본문 글자 대비를 높여줘.
레이아웃과 문구는 유지하고, 바꾼 CSS를 설명해줘.
```

수정 후 브라우저를 새로고침해서 다시 확인합니다.

### 4단계: 내가 이해한 내용 남기기 — 7분

```text
이 파일을 학습용으로 설명해줘.
1. HTML, CSS, JavaScript가 각각 담당하는 역할
2. 테마 버튼을 누른 뒤 일어나는 과정
3. 내가 직접 바꿔볼 만한 부분 3개
4. 실제로 확인한 항목과 아직 확인하지 못한 항목
설명은 짧게 하고, 학습 메모를 README.md로 작성해줘.
```

**완료 기준:** 페이지가 열리고, 버튼이 동작하고, 바뀐 부분을 한 문장으로 설명할 수 있으면 첫 실습 성공입니다.

## 4. 원하는 결과를 얻는 요청법

요청에 **목표·대상·조건·완료 기준**을 넣으면 결과를 평가하기 쉬워집니다.

```text
[목표] 어떤 문제를 해결할지
[대상] 어느 파일이나 기능을 다룰지
[조건] 지켜야 할 제약
[완료 기준] 무엇을 확인하면 끝인지
```

### 기능 추가

```text
index.html의 관심사 목록에 항목 추가 기능을 만들어줘.
입력창과 추가 버튼을 넣고, 공백만 입력하면 추가하지 않게 해줘.
기존 테마 전환 기능을 유지해줘.
정상 입력과 빈 입력을 각각 확인하는 방법을 알려줘.
```

### 오류 수정

```text
증상: 추가 버튼을 한 번 눌렀는데 목록에 두 번 표시돼.
재현: 페이지를 열고 '영화'를 입력한 뒤 추가 버튼 클릭.
기대 결과: '영화'가 한 번만 추가됨.
관련 이벤트 처리를 확인하고 원인을 설명한 뒤 최소한으로 수정해줘.
```

### 코드 이해

```text
테마 전환 코드를 설명해줘.
사용자가 버튼을 클릭한 시점부터 화면이 바뀔 때까지 순서대로 설명하고,
각 단계가 어느 코드에 해당하는지 보여줘.
```

### 작업 범위 줄이기

```text
이번에는 버튼 색상만 변경해줘.
기능 추가와 코드 구조 변경은 다음 작업으로 미뤄줘.
```

요청이 길어질수록 좋다는 뜻은 아닙니다. 필요한 정보를 명확히 주고 한 번에 하나의 결과를 확인하세요.

## 5. 자주 쓰는 명령어

### 터미널에 입력

| 명령 | 용도 |
| --- | --- |
| `claude` | 대화 시작 |
| `claude --version` | 설치 버전 확인 |
| `claude -c` | 현재 폴더의 가장 최근 대화 이어가기 |
| `claude -r` | 이전 대화 선택하여 재개 |

명령 설명: [공식 시작 가이드](https://code.claude.com/docs/en/quickstart)

설치에 문제가 있다면 터미널에서 `claude doctor`로 진단할 수 있습니다. [설치 진단](https://code.claude.com/docs/en/setup)

### Claude Code 안에 입력

| 명령 | 용도 |
| --- | --- |
| `/help` | 사용 가능한 명령 확인 |
| `/login` | 로그인 또는 계정 재인증 |
| `/clear` | 현재 대화 이력 비우기 |
| `/exit` | 종료 |

`/clear`는 파일 수정을 되돌리는 명령이 아닙니다. [세션 명령 안내](https://code.claude.com/docs/en/quickstart)

## 6. CLAUDE.md로 작업 규칙 정하기

`CLAUDE.md`는 프로젝트의 설명과 반복해서 지켜야 할 작업 규칙을 적는 Markdown 파일입니다. 프로젝트 최상위 폴더에 둘 수 있으며, `/init`으로 초안을 생성할 수 있습니다. 생성된 내용은 실제 프로젝트와 맞는지 검토하세요. [프로젝트 메모리 안내](https://code.claude.com/docs/en/memory)

실습 프로젝트라면 아래처럼 시작할 수 있습니다.

```markdown
# 프로젝트 안내

## 목적
코딩 입문자를 위한 자기소개 웹페이지입니다.

## 구성
- index.html: 화면, 스타일, 버튼 동작
- README.md: 실행 방법과 학습 메모

## 작업 규칙
- 설명은 한국어로 작성합니다.
- HTML, CSS, 기본 JavaScript만 사용합니다.
- 요청한 기능에 필요한 범위만 수정합니다.
- 새 라이브러리가 필요하면 이유를 먼저 설명합니다.

## 확인 방법
- 브라우저에서 index.html을 엽니다.
- 밝은 테마와 어두운 테마의 가독성을 확인합니다.
- 좁은 화면에서 가로 넘침이 없는지 확인합니다.
- 직접 실행하지 못한 검증은 미확인으로 보고합니다.
```

이 파일은 작업 지침이며 강제적인 권한 차단 장치는 아닙니다. 비밀번호나 API 키를 넣는 용도로 사용하지 마세요. [CLAUDE.md의 적용 방식](https://code.claude.com/docs/en/memory)

## 7. 기존 프로젝트에서 작업하기

처음 다루는 프로젝트에서는 **파악 → 작은 변경 → 검증 → 변경 기록** 순서로 진행해 보세요.

### 프로젝트 파악

```text
README와 프로젝트 설정을 읽고 다음을 정리해줘.
- 이 프로젝트가 하는 일
- 실행에 필요한 도구
- 실행 명령과 테스트 명령
- 주요 폴더 역할
아직 파일 수정이나 패키지 설치는 하지 마.
명령을 추측하지 말고 확인한 파일을 근거로 알려줘.
```

### 작은 변경과 검증

```text
회원가입 폼에서 이메일이 비어 있으면 제출되지 않도록 수정해줘.
기존 구현 방식을 따르고 관련된 코드만 변경해줘.
프로젝트에 테스트가 있으면 관련 테스트를 실행해줘.
변경 파일, 확인 결과, 미확인 항목을 정리해줘.
```

### 변경 기록

Git 저장소라면 터미널에서 아래 명령으로 변경을 살펴볼 수 있습니다.

```bash
git status
git diff
```

`git status`는 새 파일도 표시하지만, `git diff`는 기본적으로 아직 추적하지 않는 새 파일의 내용을 보여주지 않습니다. 새 파일은 편집기로 직접 열어 확인하세요.

Git이 익숙하지 않다면 연습용 복사본에서 먼저 작업해도 좋습니다. 커밋은 로컬 변경 기록이고, 푸시는 원격 저장소로 올리는 작업입니다. 파일 수정·커밋·푸시·배포를 각각 구분해서 요청하세요.

## 8. 문제 해결과 사용량 관리

| 상황 | 먼저 확인할 것 |
| --- | --- |
| `claude`를 찾을 수 없음 | 터미널을 새로 열고 재시도. 설치 결과와 PATH 안내 확인 |
| 로그인 실패 | `/login`으로 재시도하고 사용 권한이 있는 계정인지 확인 |
| 엉뚱한 파일을 살펴봄 | 실행한 프로젝트 폴더가 맞는지 확인 |
| 코드는 바뀌었는데 화면이 같음 | 저장된 파일 경로와 브라우저에서 연 파일 확인 후 새로고침 |
| 실행 명령이 실패함 | 실제 오류 메시지와 실행 명령을 함께 전달 |
| 결과가 요구사항과 다름 | 기대 동작과 실제 동작을 구체적으로 비교해서 전달 |

설치 문제는 `claude doctor` 진단과 [공식 설치 문서](https://code.claude.com/docs/en/setup)를 함께 확인하세요.

오류를 질문할 때는 다음 양식을 쓰세요.

```text
운영체제:
실행한 명령:
오류 메시지:
기대했던 결과:
이미 시도한 방법:

원인을 먼저 설명하고, 가장 작은 해결 단계부터 안내해줘.
```

로그를 공유하기 전에 비밀번호·API 키·개인정보는 지우세요.

사용량을 관리하려면 작업 범위를 작게 정하고, 관계없는 자료를 한꺼번에 붙여 넣지 않는 습관이 도움이 됩니다. 긴 작업 중에는 현재 상태와 남은 일을 문서로 정리해 두세요. 계정별 이용 한도와 비용은 해당 계정 화면에서 확인하고, 무제한 사용을 전제로 작업하지 마세요.

## 9. 일주일 학습 계획

| 날짜 | 연습 | 완료 기준 |
| --- | --- | --- |
| 1일 차 | 설치, 로그인, 첫 질문 | 프로젝트 폴더에서 대화 시작 |
| 2일 차 | 자기소개 페이지 제작 | 브라우저에서 페이지 확인 |
| 3일 차 | 버튼 또는 입력 기능 추가 | 정상 입력과 빈 입력 확인 |
| 4일 차 | 코드 설명 요청 후 직접 수정 | 문구나 색상 직접 변경 |
| 5일 차 | 작은 오류 재현과 수정 | 재현 과정과 해결 원인 설명 |
| 6일 차 | CLAUDE.md 정리 | 프로젝트 규칙과 확인 방법 기록 |
| 7일 차 | Git 변경 확인과 README 정리 | 다른 사람이 실행할 수 있게 안내 |

## 10. 한 장 체크리스트

### 작업 시작 전

- [ ] 올바른 프로젝트 폴더에서 실행했다.
- [ ] 이번에 만들거나 고칠 것을 한 문장으로 정했다.
- [ ] 중요한 기존 작업을 복사하거나 Git으로 기록했다.

### 요청할 때

- [ ] 목표와 대상을 구체적으로 적었다.
- [ ] 지켜야 할 조건과 완료 기준을 적었다.
- [ ] 오류는 재현 과정과 실제 메시지를 함께 전달했다.

### 작업이 끝난 뒤

- [ ] 수정된 파일을 확인했다.
- [ ] 결과물을 직접 열거나 실행했다.
- [ ] 정상 동작과 실패 상황을 확인했다.
- [ ] 아직 확인하지 못한 항목을 구분했다.
- [ ] 다음에 필요한 내용을 README 또는 CLAUDE.md에 남겼다.

## 공식 참고 문서

- [Claude Code 시작 가이드](https://code.claude.com/docs/en/quickstart)
- [설치·계정·진단 안내](https://code.claude.com/docs/en/setup)
- [CLAUDE.md와 프로젝트 메모리](https://code.claude.com/docs/en/memory)

설치 명령과 제품 동작은 위 공식 문서를 기준일에 확인했습니다. 실습 예제, 프롬프트, 학습 계획은 입문 학습을 위해 구성한 예시이며, 이 문서 작성 과정에서 Claude Code 설치나 예제 웹페이지 실행을 수행한 것은 아닙니다. 화면과 기능은 설치 버전 및 조직 설정에 따라 달라질 수 있습니다.
