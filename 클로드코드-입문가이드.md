# 클로드 코드(Claude Code) 입문자 가이드북

> 터미널에서 동작하는 Anthropic 공식 코딩 에이전트. 코드를 읽고, 고치고, 실행하고, 커밋까지 합니다.

---

## 목차

1. [클로드 코드란?](#1-클로드-코드란)
2. [설치와 첫 실행](#2-설치와-첫-실행)
3. [기본 사용법 — 30분 만에 익히기](#3-기본-사용법--30분-만에-익히기)
4. [핵심 단축키](#4-핵심-단축키)
5. [자주 쓰는 슬래시 명령어](#5-자주-쓰는-슬래시-명령어)
6. [CLAUDE.md — 프로젝트 기억시키기](#6-claudemd--프로젝트-기억시키기)
7. [권한 모드 이해하기](#7-권한-모드-이해하기)
8. [플랜 모드로 안전하게 작업하기](#8-플랜-모드로-안전하게-작업하기)
9. [컨텍스트 관리 (중요!)](#9-컨텍스트-관리-중요)
10. [Git과 함께 쓰기](#10-git과-함께-쓰기)
11. [한 단계 위로: 커스텀 명령어 · 서브에이전트 · 훅 · MCP](#11-한-단계-위로-커스텀-명령어--서브에이전트--훅--mcp)
12. [IDE 연동](#12-ide-연동)
13. [잘 쓰는 사람들의 7가지 습관](#13-잘-쓰는-사람들의-7가지-습관)
14. [자주 겪는 문제 해결](#14-자주-겪는-문제-해결)
15. [치트시트](#15-치트시트)

---

## 1. 클로드 코드란?

클로드 코드는 **터미널에서 실행하는 AI 코딩 에이전트**입니다. 챗봇과 결정적으로 다른 점:

| 챗봇 | 클로드 코드 |
|---|---|
| 내가 코드를 복사해서 붙여넣음 | 스스로 파일을 읽음 |
| 답변을 받아 내가 수정함 | 직접 파일을 수정함 |
| 실행은 내 몫 | 테스트·빌드·git 명령을 직접 실행함 |
| 프로젝트 전체 맥락 모름 | 저장소를 탐색해서 맥락을 파악함 |

**잘 맞는 일**

- 버그 수정, 리팩터링, 테스트 작성
- 처음 보는 코드베이스 파악하기 ("이 인증 로직 어디서 시작돼?")
- 반복 작업 (전 파일에 걸친 이름 변경, 마이그레이션)
- Git 작업 (커밋, PR 생성, 코드 리뷰)

**잘 안 맞는 일**

- 요구사항이 모호한 대규모 신규 설계 → 먼저 사람이 설계를 잡고 맡기세요
- 정답 검증이 불가능한 작업 → AI 결과물은 **항상 검증**이 필요합니다

---

## 2. 설치와 첫 실행

### 설치

```bash
# npm (Node.js 18+ 필요)
npm install -g @anthropic-ai/claude-code
```

```bash
# macOS / Linux 네이티브 설치
curl -fsSL https://claude.ai/install.sh | bash
```

```powershell
# Windows PowerShell
irm https://claude.ai/install.ps1 | iex
```

> Windows에서는 Git for Windows를 함께 설치해 두면 좋습니다. 일부 기능이 POSIX 셸을 사용합니다.

### 첫 실행

```bash
cd ~/my-project    # 반드시 프로젝트 폴더에서 실행
claude
```

처음 실행하면 로그인 절차가 진행됩니다.

- **Claude Pro / Max 구독** → 구독으로 사용 (대부분의 개인 사용자에게 유리)
- **Anthropic Console API 키** → 사용량 기반 과금

설치 상태 점검:

```bash
claude doctor
```

---

## 3. 기본 사용법 — 30분 만에 익히기

### 그냥 말을 걸면 됩니다

```
> 이 프로젝트 구조를 설명해줘

> src/auth 폴더에서 로그인 흐름을 따라가서 요약해줘

> 로그인 실패 시 에러 메시지가 안 뜨는 버그가 있어. 원인 찾아서 고쳐줘

> 방금 고친 부분에 대한 테스트를 작성하고 실행해줘
```

### 파일을 정확히 지목하기: `@`

```
> @src/api/user.ts 이 파일의 타입 정의를 정리해줘
```

`@`를 입력하면 파일 경로 자동완성이 뜹니다. **추측하게 두지 말고 파일을 직접 지목**하는 것이 속도와 정확도 모두에 좋습니다.

### 셸 명령 직접 실행: `!`

```
> !npm test
```

`!`로 시작하면 그 명령이 바로 실행되고, **출력이 대화 맥락에 들어갑니다.** 로그인·대화형 명령(`gcloud auth login` 등)을 실행할 때 특히 유용합니다.

### 메모리에 추가: `#`

```
> # 이 프로젝트는 항상 pnpm을 사용한다
```

`#`로 시작하면 CLAUDE.md에 규칙으로 저장할지 물어봅니다.

### 한 줄 실행 (헤드리스)

```bash
claude -p "이 저장소의 테스트 커버리지를 요약해줘"
claude -p "린트 에러 전부 고쳐줘" --output-format json   # CI 스크립트용
```

### 이전 대화 이어가기

```bash
claude -c          # 직전 대화 계속
claude -r          # 목록에서 골라서 재개
```

---

## 4. 핵심 단축키

| 키 | 동작 |
|---|---|
| `Shift + Tab` | **권한 모드 전환** (기본 ↔ 자동 수락 ↔ 플랜 모드) — 가장 많이 쓰게 됩니다 |
| `Esc` | 작업 중단 (대화는 유지) |
| `Esc` `Esc` | 되감기 — 이전 시점으로 롤백 |
| `Ctrl + C` | 현재 입력 취소 |
| `Ctrl + D` | 종료 |
| `Ctrl + L` | 화면 정리 |
| `Ctrl + R` | 이전 프롬프트 검색 |
| `↑` | 이전 프롬프트 불러오기 |
| `Ctrl + V` | 클립보드 이미지 붙여넣기 (스크린샷 → 디버깅) |
| `\` + `Enter` | 줄바꿈 (여러 줄 입력) |

> **Esc를 두려워하지 마세요.** 클로드가 엉뚱한 방향으로 가고 있으면 즉시 Esc를 눌러 멈추고 방향을 다시 잡는 게, 끝까지 두고 보다가 되돌리는 것보다 훨씬 빠릅니다.

---

## 5. 자주 쓰는 슬래시 명령어

| 명령어 | 설명 |
|---|---|
| `/help` | 전체 명령어 목록 |
| `/init` | 프로젝트를 분석해 **CLAUDE.md 자동 생성** — 새 프로젝트에서 제일 먼저 |
| `/clear` | 대화 맥락 완전 초기화 — **작업 단위가 바뀔 때마다 실행** |
| `/compact` | 맥락을 요약해서 압축 (작업은 이어감) |
| `/context` | 현재 컨텍스트 사용량 확인 |
| `/model` | 모델 변경 (Opus / Sonnet / Haiku) |
| `/config` | 설정 화면 |
| `/permissions` | 권한 규칙 관리 |
| `/memory` | CLAUDE.md 편집 |
| `/review` | 코드 리뷰 |
| `/cost` | 이번 세션 사용 비용 |
| `/usage` | 사용량/한도 확인 |
| `/status` | 현재 세션 상태 |
| `/mcp` | MCP 서버 관리 |
| `/agents` | 서브에이전트 관리 |
| `/hooks` | 훅 설정 |
| `/rewind` | 체크포인트로 되돌리기 |
| `/export` | 대화 내보내기 |
| `/add-dir` | 작업 디렉터리 추가 (모노레포에서 유용) |
| `/vim` | Vim 키 바인딩 |
| `/doctor` | 설치 상태 진단 |

---

## 6. CLAUDE.md — 프로젝트 기억시키기

`CLAUDE.md`는 **매 세션 자동으로 읽히는 프로젝트 설명서**입니다. 입문자가 얻는 체감 효과가 가장 큰 파일입니다.

```bash
claude
> /init          # 자동 생성
```

### 위치별 역할

| 경로 | 범위 |
|---|---|
| `./CLAUDE.md` | 프로젝트 공용 (git에 커밋) |
| `./CLAUDE.local.md` | 나만의 로컬 규칙 (gitignore) |
| `~/.claude/CLAUDE.md` | 모든 프로젝트 공통 |

### 좋은 CLAUDE.md 예시

```markdown
# 프로젝트 개요
Next.js 14 + TypeScript 기반 사내 대시보드.

## 명령어
- 개발 서버: `pnpm dev`
- 테스트: `pnpm test` (Vitest)
- 린트: `pnpm lint` — **커밋 전 반드시 실행**
- 타입 체크: `pnpm typecheck`

## 규칙
- 패키지 매니저는 pnpm만 사용 (npm/yarn 금지)
- 컴포넌트는 함수형 + named export
- API 호출은 반드시 `src/lib/api-client.ts`를 경유
- `src/generated/` 는 자동 생성 파일 — 직접 수정 금지
- 커밋 메시지는 Conventional Commits 형식

## 주의사항
- `migrations/` 수정 시 반드시 나에게 먼저 확인
```

**요령**

- 짧고 구체적으로. 장황한 문서는 오히려 무시됩니다
- "하지 마라"보다 "이렇게 해라"가 효과적
- 클로드가 같은 실수를 두 번 하면 → 그때 CLAUDE.md에 규칙 추가

---

## 7. 권한 모드 이해하기

`Shift + Tab`으로 전환합니다.

| 모드 | 동작 | 언제 |
|---|---|---|
| **기본(default)** | 파일 수정·명령 실행마다 승인 요청 | 처음 배울 때, 낯선 저장소 |
| **자동 수락(acceptEdits)** | 파일 수정은 자동 승인 | 방향이 확실할 때 |
| **플랜(plan)** | **읽기만** 하고 계획 제시 | 큰 작업 시작 전 |
| **bypassPermissions** | 전부 자동 | 격리된 환경/컨테이너에서만 |

> ⚠️ `bypassPermissions`(`--dangerously-skip-permissions`)는 편하지만 위험합니다. 되돌릴 수 없는 명령까지 실행됩니다. **git으로 관리되는 저장소**에서, 그리고 가급적 컨테이너 안에서만 쓰세요.

### 매번 묻는 게 귀찮다면

```
> /permissions
```

여기서 자주 쓰는 읽기 전용 명령을 허용 목록에 추가하세요. 예: `Bash(npm test:*)`, `Bash(git status)`, `Bash(git diff:*)`

---

## 8. 플랜 모드로 안전하게 작업하기

큰 작업의 정석 흐름입니다.

```
1) Shift+Tab 을 눌러 플랜 모드 진입
2) "결제 모듈을 Stripe에서 Toss로 교체하려고 해. 코드 읽고 계획 세워줘"
3) 클로드가 코드만 읽고 → 단계별 계획 제시
4) 계획을 읽고 수정 요청 ("2단계는 건너뛰고, 테스트부터 먼저 짜줘")
5) 승인 → 실행
```

**왜 중요한가:** 잘못된 방향으로 500줄을 수정한 뒤 되돌리는 것보다, 계획 단계에서 3줄을 고치는 게 압도적으로 쌉니다.

---

## 9. 컨텍스트 관리 (중요!)

입문자가 가장 많이 놓치는 부분입니다. 클로드 코드는 **대화가 길어질수록 느려지고 산만해집니다.**

### 규칙 3가지

**① 작업이 끝나면 `/clear`**

```
로그인 버그 수정 완료 → /clear → 이제 API 리팩터링 시작
```

관련 없는 이전 맥락은 방해만 됩니다.

**② `/context`로 주기적 확인**

사용량이 높으면 `/compact` 또는 `/clear`.

**③ 큰 탐색은 서브에이전트에게**

```
> 이 저장소 전체에서 deprecated된 API 사용처를 찾아줘. 서브에이전트로 검색해줘
```

검색 결과 수천 줄이 내 대화창이 아니라 서브에이전트 쪽에 쌓입니다.

---

## 10. Git과 함께 쓰기

클로드 코드는 git을 아주 잘 다룹니다.

```
> 변경사항 확인하고 의미 단위로 나눠서 커밋해줘

> 지금 브랜치 변경사항으로 PR 만들어줘. 설명은 한국어로

> main과 비교해서 이 브랜치 코드 리뷰해줘

> 이 함수를 마지막으로 수정한 커밋과 이유를 찾아줘
```

**안전 수칙**

- 클로드에게 작업을 맡기기 전에 **먼저 커밋**해두세요. 되돌릴 지점이 생깁니다
- `main` 브랜치에서 바로 작업시키지 말고 브랜치를 파세요
- 커밋·푸시는 명시적으로 요청할 때만 하도록 되어 있지만, 늘 `git diff`로 직접 확인하는 습관을 들이세요

---

## 11. 한 단계 위로: 커스텀 명령어 · 서브에이전트 · 훅 · MCP

### ① 커스텀 슬래시 명령어

`.claude/commands/` 에 마크다운 파일을 두면 그대로 명령어가 됩니다.

`.claude/commands/fix-issue.md`:

```markdown
---
description: GitHub 이슈를 읽고 수정한다
argument-hint: <이슈번호>
---

GitHub 이슈 #$1 을 `gh issue view $1` 로 읽고,
원인을 파악한 뒤 수정하고 테스트를 추가해줘.
```

사용: `/fix-issue 42`

- `$ARGUMENTS` — 전체 인자
- `$1`, `$2` — 개별 인자

### ② 서브에이전트

`.claude/agents/` 에 전문 에이전트를 정의합니다. 별도 컨텍스트에서 돌아서 메인 대화를 오염시키지 않습니다.

`.claude/agents/test-writer.md`:

```markdown
---
name: test-writer
description: 테스트 작성 전문. 새 기능에 테스트가 필요할 때 사용.
tools: Read, Write, Edit, Bash, Grep, Glob
---

너는 테스트 작성 전문가다. Vitest를 사용하고,
엣지 케이스를 반드시 포함하며, 작성 후 실제로 실행해 통과를 확인한다.
```

`/agents` 로 대화형 생성·관리도 가능합니다.

### ③ 훅 (Hooks)

특정 시점에 **자동으로** 셸 명령을 실행합니다. "매번 ~해줘" 같은 요구는 메모리가 아니라 훅으로 해결해야 확실합니다.

주요 이벤트: `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `Stop`, `SessionStart`, `PreCompact`

예시 — 파일 수정 후 자동 포맷팅 (`.claude/settings.json`):

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "pnpm prettier --write $CLAUDE_FILE_PATHS" }
        ]
      }
    ]
  }
}
```

`/hooks` 로 UI에서 설정할 수도 있습니다.

### ④ MCP — 외부 도구 연결

MCP(Model Context Protocol)로 클로드를 외부 시스템에 연결합니다. GitHub, Slack, DB, Figma, 브라우저 등.

```bash
claude mcp add --transport http github https://api.githubcopilot.com/mcp
claude mcp list
```

프로젝트 공용 설정은 루트의 `.mcp.json`에 저장되어 팀원과 공유됩니다.

```
> /mcp     # 연결 상태 확인 및 인증
```

### ⑤ 스킬 (Skills)

`.claude/skills/<이름>/SKILL.md` 에 작업 절차를 문서로 넣어두면, 클로드가 관련 상황에서 알아서 불러 씁니다. 배포 절차, 리뷰 체크리스트 같은 반복 워크플로에 적합합니다.

---

## 12. IDE 연동

**VS Code / Cursor / Windsurf**

- 확장 마켓플레이스에서 "Claude Code" 설치
- 또는 VS Code 통합 터미널에서 `claude` 실행 → 자동 연동
- 에디터에서 선택한 코드가 자동으로 맥락에 포함되고, 수정 사항이 diff 뷰로 표시됩니다

**JetBrains (IntelliJ, PyCharm, WebStorm 등)**

- 플러그인 마켓플레이스에서 "Claude Code" 설치

**기타**

- 데스크톱 앱(Mac/Windows), 웹(claude.ai/code)에서도 사용 가능

---

## 13. 잘 쓰는 사람들의 7가지 습관

**1. 구체적으로 말한다**

- ❌ "로그인 고쳐줘"
- ✅ "`@src/auth/login.ts` 에서 비밀번호 틀렸을 때 401 대신 500이 떨어져. 재현 후 원인 찾아서 고치고 테스트 추가해줘"

**2. 먼저 읽게 하고, 그다음 쓰게 한다**

"고치기 전에 관련 파일들 읽고 원인부터 설명해줘"

**3. 검증 수단을 함께 준다**

"수정 후 `pnpm test` 돌려서 통과 확인해줘" — 클로드가 스스로 결과를 확인하고 고칠 수 있게 됩니다.

**4. TDD를 시킨다**

"먼저 실패하는 테스트를 작성하고, 그 다음 통과시키는 구현을 해줘" — 품질이 눈에 띄게 올라갑니다.

**5. 작업 단위를 쪼갠다**

한 번에 하나. 끝나면 `/clear`. 거대한 요청 하나보다 명확한 요청 다섯 개가 낫습니다.

**6. 스크린샷을 붙여넣는다**

UI 버그는 `Ctrl+V`로 스크린샷을 넣으면 설명보다 훨씬 정확합니다.

**7. 결과를 반드시 본인이 검토한다**

`git diff`를 읽으세요. 클로드가 "완료했습니다"라고 해도, 머지 책임은 사람에게 있습니다.

---

## 14. 자주 겪는 문제 해결

| 증상 | 해결 |
|---|---|
| 응답이 느리고 엉뚱함 | `/clear` 후 다시 시작. 컨텍스트가 오염된 상태 |
| 같은 실수를 반복함 | CLAUDE.md에 규칙 추가 |
| 매번 권한을 물어봄 | `/permissions` 에서 안전한 명령 허용 목록 등록 |
| 엉뚱한 파일을 수정함 | `@경로`로 파일을 직접 지목 |
| 작업이 산으로 감 | `Esc`로 즉시 중단 → `Esc Esc`로 되감기 |
| 설치·연결 문제 | `claude doctor`, `/status` |
| 사용량 한도 걱정 | `/usage`, `/cost` 로 확인. 단순 작업은 `/model`로 Haiku 전환 |
| MCP 서버가 안 붙음 | `/mcp` 에서 인증 상태 확인 |

---

## 15. 치트시트

```
─── 실행 ────────────────────────────────────
claude                    대화 시작
claude "프롬프트"          프롬프트와 함께 시작
claude -p "프롬프트"       한 번 실행하고 종료 (헤드리스)
claude -c                 직전 대화 이어가기
claude -r                 대화 목록에서 재개
claude doctor             설치 진단
claude mcp add ...        MCP 서버 추가

─── 입력 접두사 ──────────────────────────────
@경로                     파일 지목 (자동완성)
!명령                     셸 명령 실행
#내용                     메모리에 규칙 추가
/명령                     슬래시 명령

─── 단축키 ──────────────────────────────────
Shift+Tab                 권한 모드 전환 ★
Esc                       중단
Esc Esc                   되감기
Ctrl+V                    이미지 붙여넣기
Ctrl+R                    프롬프트 검색
Ctrl+L / Ctrl+D           화면 정리 / 종료

─── 필수 슬래시 명령 ─────────────────────────
/init        CLAUDE.md 생성 (새 프로젝트 첫 단계)
/clear       맥락 초기화 (작업 전환 시 필수)
/context     컨텍스트 사용량 확인
/model       모델 변경
/permissions 권한 관리
/review      코드 리뷰
/cost        비용 확인
/help        전체 목록

─── 설정 파일 위치 ───────────────────────────
~/.claude/settings.json       내 전역 설정
~/.claude/CLAUDE.md           내 전역 규칙
.claude/settings.json         프로젝트 공용 설정 (커밋)
.claude/settings.local.json   프로젝트 개인 설정 (gitignore)
.claude/commands/*.md         커스텀 슬래시 명령
.claude/agents/*.md           서브에이전트
.claude/skills/*/SKILL.md     스킬
./CLAUDE.md                   프로젝트 규칙
./.mcp.json                   MCP 서버 (팀 공유)
```

---

## 첫 주 실습 코스

| 일차 | 할 일 |
|---|---|
| 1일차 | `claude` 실행 → `/init` → "이 프로젝트 구조 설명해줘" |
| 2일차 | 작은 버그 하나 수정시키고 `git diff`로 직접 검토 |
| 3일차 | `Shift+Tab` 플랜 모드로 리팩터링 계획 세우게 하기 |
| 4일차 | CLAUDE.md에 우리 팀 규칙 5줄 추가 |
| 5일차 | 커밋·PR 생성 맡겨보기 |
| 6일차 | 커스텀 슬래시 명령어 하나 만들기 |
| 7일차 | 자주 하는 반복 작업을 훅이나 서브에이전트로 자동화 |

---

## 더 읽을거리

- 공식 문서: <https://docs.claude.com/en/docs/claude-code>
- 베스트 프랙티스: <https://www.anthropic.com/engineering/claude-code-best-practices>
- 터미널에서: `/help`

---

*마지막 한마디: 클로드 코드는 "명령을 받는 도구"가 아니라 "함께 일하는 동료"에 가깝습니다. 맥락을 주고, 방향을 잡아주고, 결과를 검토하세요. 그게 전부입니다.*
